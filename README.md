# habit-tracking

A React + Tailwind habit tracker with local storage persistence.

## Run locally

```bash
npm install
npm run dev
```

The app runs on `http://localhost:3000` and starts on a welcome screen. Click **Let's Go** to open the tracker view.
